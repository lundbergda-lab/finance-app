# Anthropic API key used for statement parsing and AI categorization.
# If set, the browser never needs a key (Settings shows "Using server key").
# If left empty, the key entered in Settings is used as fallback.
ANTHROPIC_API_KEY=
